# CHANGELOG.md is generated by semantic-release (@semantic-release/changelog) and
# committed with [skip ci], so its formatting is never validated and it uses a
# style Prettier rewrites (`*` bullets, double blank lines). It is regenerated on
# every release, so formatting it by hand would only recur — exclude it instead.
CHANGELOG.md
